# Bloqueia acesso HTTP direto a esta pasta (defesa extra, alem do redirecionamento
# para public/ feito no .htaccess da raiz).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
